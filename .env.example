# Hatchery configuration
# Copy to .env and fill in your values.

# GitHub App credentials (required for scoped credentials)
# Installation IDs are in config.json (see config.example.json)
HATCHERY_GITHUB_CLIENT_ID=
HATCHERY_GITHUB_APP_KEY=private-key.pem

# GitHub username (for SSH key injection from github.com/<user>.keys)
HATCHERY_GITHUB_USER=

# Headscale/Tailscale (required for spawn)
HATCHERY_HEADSCALE_AUTH_KEY=
HATCHERY_TAILSCALE_DOMAIN=headscale.levinkeller.de

# Socket directory for credential service (default: ~/.hatchery/sockets)
# HATCHERY_SOCKET_DIR=
